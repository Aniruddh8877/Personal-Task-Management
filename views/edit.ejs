<!DOCTYPE html>
<html lang="en">

<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title><%= title %> | Edit Task</title>
     <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen font-sans">
     <div class="max-w-4xl mx-auto py-12 px-6">

          <!-- Back Button -->
          <div class="mb-8">
               <a href="/" class="inline-flex items-center text-blue-500 hover:underline text-lg font-medium">
                    ← Back to Home
               </a>
          </div>

          <!-- Edit Task Form -->
          <div class="bg-zinc-800 p-8 rounded-2xl shadow-xl border border-zinc-700">
               <h1 class="text-3xl sm:text-4xl font-bold text-blue-400 mb-6">
                    Edit Task
               </h1>

               <form action="/update" method="POST" class="space-y-6">
                    <!-- Hidden field to store original filename -->
                    <input type="hidden" name="originalTitle" value="<%= title %>">

                    <div>
                         <label for="title" class="block text-sm font-medium text-zinc-300 mb-2">Title</label>
                         <input
                              class="w-full py-4 px-4 bg-zinc-700 text-white rounded-lg border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
                              type="text" name="title" id="title" required
                              value="<%= title.replace('.txt', '') %>" placeholder="Enter your title">
                    </div>

                    <div>
                         <label for="description"
                              class="block text-sm font-medium text-zinc-300 mb-2">Description</label>
                         <textarea
                              class="w-full h-40 py-4 px-4 bg-zinc-700 text-white rounded-lg border border-zinc-600 resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
                              name="description" id="description" placeholder="Enter your description"
                              required><%= description %></textarea>
                    </div>

                    <div class="text-right">
                         <button type="submit"
                              class="bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded-lg text-white font-semibold transition">
                              💾 Update Task
                         </button>
                    </div>
               </form>
          </div>

     </div>

     <script src="/js/script.js"></script>
</body>

</html>
